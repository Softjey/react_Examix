enum Routes {
  START = '/',
  LOGIN = '/login',
  JOIN = '/join',
  NOT_FOUND = '*',
  QUIZ = '/quiz',
  WAITING = '/waiting',
  HOME = '/home',
  TESTS_LIBRARY = '/tests',
  TEST = '/test',
  CREATE_TEST = '/create-test',
  EXAMS_HISTORY = '/history',
  SETTINGS = '/settings',
  EXAM = '/exam',
}

export default Routes;
