import { CSSObject } from '@emotion/react';

export const center: CSSObject = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
};

export const columnCenter: CSSObject = {
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
};
